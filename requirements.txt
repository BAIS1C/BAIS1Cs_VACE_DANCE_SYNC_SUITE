torch>=1.9.0          # Deep learning framework (used by DWPose and pose tensors)
numpy>=1.20.0         # Array/matrix operations and pose tensor math
onnxruntime>=1.12.0   # ONNX model inference for DWPose detection
opencv-python>=4.5.0  # Computer vision (pose visualization, stickman rendering)
decord>=0.6.0         # Fast video decoding/reading for source videos
tqdm>=4.62.0          # Progress bars for pose extraction
Pillow>=8.0.0         # Image processing (if used for previews/thumbnails)
librosa>=0.9.0        # Audio analysis (BPM detection, beat tracking, tempo estimation)
soundfile>=0.10.0     # Audio file I/O (reading WAV files from FFmpeg extraction)
matplotlib>=3.5.0     # Plotting and color utilities (used in pose visualization)
scipy>=1.7.0          # Scientific computing (signal processing for audio analysis)